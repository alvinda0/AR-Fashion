# Architecture & Flow Diagram

## 📐 Application Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        Flutter App                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────┐         ┌──────────────┐                │
│  │   Main.dart  │────────▶│ Home Screen  │                │
│  └──────────────┘         └──────┬───────┘                │
│                                   │                         │
│                    ┌──────────────┴──────────────┐         │
│                    │                             │         │
│           ┌────────▼────────┐         ┌─────────▼────────┐│
│           │  Gallery Screen │         │ AR Camera Screen ││
│           └─────────────────┘         └──────────────────┘│
│                                              │             │
│                                              │             │
│                    ┌─────────────────────────┘             │
│                    │                                       │
│         ┌──────────▼──────────┐                           │
│         │  3D Model Viewer    │                           │
│         │  (ModelViewer)      │                           │
│         └─────────────────────┘                           │
│                    │                                       │
│         ┌──────────▼──────────┐                           │
│         │   GLB Model Files   │                           │
│         │   (assets/glb/)     │                           │
│         └─────────────────────┘                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 User Flow

```
┌─────────────┐
│  App Start  │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│   Home Screen   │
└──────┬──────────┘
       │
       │ Tap "AR Camera"
       ▼
┌─────────────────────────────────┐
│     AR Camera Screen            │
│  ┌───────────────────────────┐  │
│  │   Camera Preview          │  │
│  │   (Full Screen)           │  │
│  └───────────────────────────┘  │
│  ┌───────────────────────────┐  │
│  │   Fashion Items List      │  │
│  │   (Bottom Overlay)        │  │
│  └───────────────────────────┘  │
└──────┬──────────────────────────┘
       │
       │ Tap Fashion Item
       ▼
┌─────────────────────────────────┐
│   3D Model Overlay              │
│  ┌───────────────────────────┐  │
│  │   ModelViewer             │  │
│  │   - Drag to rotate        │  │
│  │   - Pinch to zoom         │  │
│  │   - Auto-rotate           │  │
│  │   - AR mode button        │  │
│  └───────────────────────────┘  │
│  [X] Close Button               │
└──────┬──────────────────────────┘
       │
       │ Tap Close or Back
       ▼
┌─────────────────┐
│  Back to Camera │
└─────────────────┘
```

## 🏗️ Component Structure

### AR Camera Screen

```
ARCameraScreen (StatefulWidget)
│
├── State Variables
│   ├── _cameraController (CameraController?)
│   ├── _cameras (List<CameraDescription>)
│   ├── _isCameraInitialized (bool)
│   ├── _isLoading (bool)
│   ├── _fashionItems (List<Map<String, String>>)
│   └── _selectedItemId (String?)
│
├── Methods
│   ├── initState() → _initializeCamera()
│   ├── dispose() → cleanup camera
│   └── _initializeCamera() → setup camera
│
└── UI Components (Stack)
    ├── CameraPreview (Full screen)
    ├── 3D Model Overlay (conditional)
    │   └── ModelViewer widget
    ├── Back Button (top left)
    ├── Close Button (top right, conditional)
    └── Fashion Items List (bottom)
        └── ListView.builder
            └── GestureDetector (tap handler)
```

## 📊 Data Flow

```
┌──────────────────┐
│  Fashion Items   │
│  (List<Map>)     │
└────────┬─────────┘
         │
         │ Contains:
         │ - id
         │ - name
         │ - image path
         │ - model path (GLB)
         │
         ▼
┌──────────────────┐
│  User Taps Item  │
└────────┬─────────┘
         │
         │ setState()
         ▼
┌──────────────────────┐
│ _selectedItemId = id │
└────────┬─────────────┘
         │
         │ Widget Rebuild
         ▼
┌──────────────────────┐
│  Show 3D Overlay     │
└────────┬─────────────┘
         │
         │ ModelViewer loads
         ▼
┌──────────────────────┐
│  Load GLB from       │
│  assets/glb/         │
└────────┬─────────────┘
         │
         │ WebGL Rendering
         ▼
┌──────────────────────┐
│  Display 3D Model    │
│  with Controls       │
└──────────────────────┘
```

## 🔌 Dependencies Flow

```
┌─────────────────────────────────────────────────────┐
│                   Flutter App                       │
└─────────────────────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   camera     │ │model_viewer  │ │ google_ml_kit│
│   package    │ │    _plus     │ │   package    │
└──────────────┘ └──────────────┘ └──────────────┘
        │               │               │
        ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│   Camera     │ │   WebView    │ │   ML Kit     │
│   Hardware   │ │   (3D View)  │ │   APIs       │
└──────────────┘ └──────────────┘ └──────────────┘
```

## 🎨 UI Layer Structure

```
Scaffold
└── Stack (Full Screen)
    ├── Layer 1: Camera Preview
    │   └── CameraPreview widget
    │
    ├── Layer 2: 3D Model Overlay (conditional)
    │   └── Container (semi-transparent background)
    │       └── Container (white rounded box)
    │           └── ModelViewer
    │
    ├── Layer 3: Back Button
    │   └── IconButton (top left)
    │
    ├── Layer 4: Close Button (conditional)
    │   └── IconButton (top right)
    │
    └── Layer 5: Fashion Items List
        └── Container (gradient background)
            └── ListView.builder (horizontal)
                └── GestureDetector
                    └── Column
                        ├── Image (thumbnail)
                        └── Text (name)
```

## 🔄 State Management

```
┌─────────────────────────────────────────────────┐
│           ARCameraScreen State                  │
├─────────────────────────────────────────────────┤
│                                                 │
│  _selectedItemId: String?                      │
│  ├── null → No model shown                     │
│  └── "item_id" → Show model for this item      │
│                                                 │
│  _isCameraInitialized: bool                    │
│  ├── false → Show loading                      │
│  └── true → Show camera preview                │
│                                                 │
│  _isLoading: bool                              │
│  ├── true → Show CircularProgressIndicator     │
│  └── false → Show main UI                      │
│                                                 │
└─────────────────────────────────────────────────┘

State Changes:
1. initState() → _isLoading = true
2. Camera initialized → _isLoading = false, _isCameraInitialized = true
3. User taps item → _selectedItemId = item['id']
4. User taps close → _selectedItemId = null
```

## 📱 Platform Integration

```
┌─────────────────────────────────────────────────┐
│              Flutter Framework                  │
└─────────────────┬───────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│   Android    │    │     iOS      │
└──────────────┘    └──────────────┘
        │                   │
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│   ARCore     │    │   ARKit      │
│  (Optional)  │    │  (Optional)  │
└──────────────┘    └──────────────┘
```

## 🗂️ File Structure

```
lib/
├── main.dart
│   └── MaterialApp
│       └── Home Screen
│
└── screens/
    ├── ar_camera_screen.dart
    │   ├── ARCameraScreen (Widget)
    │   ├── _ARCameraScreenState (State)
    │   ├── _fashionItems (Data)
    │   └── UI Components
    │
    └── gallery_screen.dart
        └── Gallery UI

assets/
├── fbx/                    # Source FBX files
│   ├── dayana blue fbx.fbx
│   ├── nayra black fbx.fbx
│   └── ...
│
├── glb/                    # Converted GLB files (REQUIRED)
│   ├── dayana_blue.glb
│   ├── nayra_black.glb
│   └── ...
│
└── images/                 # Thumbnails
    ├── dayana.jpg
    ├── nayra.jpg
    └── ...
```

## 🔐 Permissions Flow

```
┌─────────────────┐
│   App Launch    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Check Camera    │
│ Permission      │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│Granted │ │ Denied │
└───┬────┘ └───┬────┘
    │          │
    │          ▼
    │    ┌──────────────┐
    │    │ Show Error   │
    │    │ or Request   │
    │    └──────────────┘
    │
    ▼
┌─────────────────┐
│ Initialize      │
│ Camera          │
└─────────────────┘
```

## 🎯 Model Loading Flow

```
┌──────────────────┐
│ User Taps Item   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ setState()       │
│ _selectedItemId  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Widget Rebuild   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ ModelViewer      │
│ Created          │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Load GLB File    │
│ from Assets      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Parse GLB        │
│ (WebGL)          │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Render 3D Model  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Enable Controls  │
│ - Rotate         │
│ - Zoom           │
│ - AR Mode        │
└──────────────────┘
```

## 🔧 Build Process

```
┌──────────────────┐
│  flutter build   │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Compile Dart     │
│ to Native        │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Bundle Assets    │
│ - Images         │
│ - GLB Models     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Package APK/IPA  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Install on       │
│ Device           │
└──────────────────┘
```

## 📊 Performance Considerations

```
┌─────────────────────────────────────────────────┐
│              Performance Factors                │
├─────────────────────────────────────────────────┤
│                                                 │
│  1. Model Size                                 │
│     ├── Polygon Count (< 50K recommended)      │
│     ├── Texture Size (1024x1024 max)           │
│     └── File Size (< 2MB recommended)          │
│                                                 │
│  2. Camera Resolution                          │
│     └── ResolutionPreset.medium (balanced)     │
│                                                 │
│  3. Rendering                                  │
│     ├── WebGL (hardware accelerated)           │
│     └── Auto-rotate (optional, can disable)    │
│                                                 │
│  4. Memory Usage                               │
│     ├── Camera buffer                          │
│     ├── 3D model in memory                     │
│     └── Texture cache                          │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 🎨 Styling & Theme

```
Color Scheme:
├── Primary: Color(0xFF00796B) - Teal
├── Background: Colors.black
├── Overlay: Colors.black.withAlpha(0.3)
├── Model Background: Color(0xFFEEEEEE)
└── Text: Colors.white / Colors.black

Layout:
├── Camera: Full screen
├── 3D Viewer: 90% width, 70% height
├── Items List: 180px height
└── Item Thumbnail: 110x110px
```

---

This architecture provides a clear separation of concerns and maintainable code structure for the Fashion AR application.
